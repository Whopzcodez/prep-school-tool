# PrepSchool Bootstrap Pack

Copy these files into the root of `~/projects/prep-school-tool`.

To avoid overwriting any existing files:

```bash
cp -rn prepschool-bootstrap/. ~/projects/prep-school-tool/
```

Then review:

```bash
cd ~/projects/prep-school-tool
git status --short
```
